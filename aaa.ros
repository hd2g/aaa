#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '() :silent t)
  )

(defpackage :ros.script.aaa.3893549833
  (:use :cl))
(in-package :ros.script.aaa.3893549833)

(defun main (&rest argv)
  (declare (ignorable argv))
  (format t "ok~%"))
;;; vim: set ft=lisp lisp:
